DKY.tw IDEA Box API 設定手把手教學

你現在要做的事情其實只有 3 件：

1. 去 Google 拿 Gemini API Key
2. 去 Cloudflare 把 API Key 填進 Secrets / Variables
3. 重新部署網站，打開 /ideabox 測試

重要提醒：

API Key 絕對不要貼到 GitHub、index.html、app.js、README 裡。
我們現在的程式已經改好，API Key 只會放在 Cloudflare 後台。


一、取得 Gemini API Key

1. 打開 Google AI Studio：
   https://aistudio.google.com/

2. 用你的 Google 帳號登入。

3. 找到 Get API key 或 API keys。

4. 建立一組新的 API Key。

5. 複製這組 Key。

你會拿到類似這種格式：

AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxx

這個 Key 等一下只貼到 Cloudflare，不要貼到 GitHub。


二、到 Cloudflare 設定 Secrets / Variables

1. 打開 Cloudflare：
   https://dash.cloudflare.com/

2. 左側選單進入 Workers & Pages。

3. 點你的 Pages 專案。

如果你不確定是哪一個，通常會是 dky-tools、dky-tw、dky 或類似名字。

4. 點上方或左側的 Settings。

5. 找 Variables and Secrets。

6. 先選 Production。

7. 新增以下 5 個設定。


第一個：GEMINI_API_KEY

Name:
GEMINI_API_KEY

Value:
貼上你剛剛從 Google AI Studio 複製的 API Key

Type:
Secret

說明：
這個一定要選 Secret，因為它是敏感資料。


第二個：GEMINI_MODEL

Name:
GEMINI_MODEL

Value:
gemini-2.5-flash

Type:
Variable

說明：
這是使用哪個 Gemini 模型。先用這個就好，成本和速度比較適合。


第三個：GEMINI_MAX_OUTPUT_TOKENS

Name:
GEMINI_MAX_OUTPUT_TOKENS

Value:
4096

Type:
Variable

說明：
這是限制 AI 一次最多產生多少內容，避免費用失控。


第四個：GEMINI_TIMEOUT_MS

Name:
GEMINI_TIMEOUT_MS

Value:
25000

Type:
Variable

說明：
這代表最多等 AI 25 秒。超過就中止，避免網站卡住。


第五個：ALLOWED_ORIGIN

Name:
ALLOWED_ORIGIN

Value:
先填你的網站網址。

如果你的正式網址是：
https://dky.tw

就填：
https://dky.tw

如果你也會用 www：
https://dky.tw,https://www.dky.tw

如果你還沒有正式網域，先用 Cloudflare Pages 給你的網址，例如：
https://你的專案.pages.dev

Type:
Variable

說明：
這是限制只有你的網站可以呼叫 API。可以降低被別人拿去亂用的風險。

如果你現在不確定 Pages 網址是多少，可以先暫時不要設定 ALLOWED_ORIGIN。
等網站正常後，再回來補上。


三、Preview 環境也要設定嗎？

如果 Cloudflare 有 Preview 環境，建議也設定一次。

做法：

1. 還是在 Variables and Secrets。
2. 把環境切到 Preview。
3. 重複新增上面 5 個設定。

Preview 的 ALLOWED_ORIGIN 通常要填 Cloudflare Pages 的預覽網址。

如果你只是要正式站能用，先設定 Production 就可以。


四、重新部署 Cloudflare Pages

設定完 Variables / Secrets 之後，要重新部署一次，新的設定才會生效。

做法：

1. 回到 Cloudflare Pages 專案。
2. 找 Deployments。
3. 點最新的一次部署。
4. 找 Retry deployment 或 Redeploy。
5. 等它變成 Success。


五、測試 IDEA Box

部署成功後：

1. 打開你的網站。
2. 進入：
   /ideabox

例如：
https://dky.tw/ideabox

3. 填：
   提案名稱
   構想說明

4. 按生成提案書。

如果成功：

畫面會產生 IDEA Box 提案內容。
按下載 Word 會下載 .docx 檔。

如果失敗：

畫面會出現本機範本，或 Cloudflare Function 回錯誤。
這時通常是以下原因：

1. GEMINI_API_KEY 沒設定
2. API Key 貼錯
3. ALLOWED_ORIGIN 網址填錯
4. Cloudflare 設定後還沒重新部署


六、如果 ALLOWED_ORIGIN 設錯怎麼辦？

如果按生成後一直失敗，可以先把 ALLOWED_ORIGIN 刪掉，重新部署，再測一次。

成功後，再回來設定正確網址。

常見正確值：

正式網域：
https://dky.tw

有 www：
https://dky.tw,https://www.dky.tw

Cloudflare Pages 網址：
https://你的專案.pages.dev

本機 wrangler 測試：
http://localhost:8788


七、本機測試用 .dev.vars

如果你要在自己電腦測 Cloudflare Function，可以在這個位置建立檔案：

/Users/jy/DKY/dky_tw/.dev.vars

內容如下：

GEMINI_API_KEY="你的 Gemini API Key"
GEMINI_MODEL="gemini-2.5-flash"
GEMINI_MAX_OUTPUT_TOKENS="4096"
GEMINI_TIMEOUT_MS="25000"
ALLOWED_ORIGIN="http://localhost:8788"

注意：

.dev.vars 不要推到 GitHub。
目前 .gitignore 已經有擋 .dev.vars 和 .env。


八、推 GitHub 前檢查

在 /Users/jy/DKY 執行：

git status --short

確認沒有 .env、.dev.vars、API Key 相關檔案出現。

然後再提交：

git add .
git commit -m "feat: secure ideabox api and docx export"
git push origin main


九、最簡短版

你要填進 Cloudflare 的就是這些：

GEMINI_API_KEY
你的 Gemini API Key，類型選 Secret

GEMINI_MODEL
gemini-2.5-flash

GEMINI_MAX_OUTPUT_TOKENS
4096

GEMINI_TIMEOUT_MS
25000

ALLOWED_ORIGIN
https://dky.tw

如果還沒有正式網址，ALLOWED_ORIGIN 可以先不填。


十、如果出現「無法將變數新增到只有靜態資產的 Worker 中」

你看到這句話，意思是：

Cloudflare 目前把你的專案當成「只有 HTML/CSS/JS 的純靜態 Worker」。
這種專案沒有後端 Worker 程式，所以 Cloudflare 不讓你新增後端環境變數。

我們現在這份程式是用：

Cloudflare Pages + Pages Functions

不是只有 Static Assets 的 Worker。

所以你有兩種做法。


建議做法 A：改用 Cloudflare Pages

這是目前最適合你的方式，因為程式已經放好了：

/Users/jy/DKY/dky_tw/functions/api/ideabox.js

請照這樣做：

1. 打開 Cloudflare Dashboard。

2. 進 Workers & Pages。

3. 不要點你現在那個只有 Static Assets 的 Worker。

4. 找 Pages。

5. 建立或進入你的 Pages 專案。

6. Pages 專案要從 GitHub repo 連進來：
   https://github.com/a9181873/dky-tools

7. 部署設定建議如下：

Project name:
dky-tools

Production branch:
main

Root directory:
dky_tw

Build command:
留空

Build output directory:
/

8. 部署完成後，再去這個 Pages 專案裡設定：

Settings -> Variables and Secrets

這時候就可以新增：

GEMINI_API_KEY
GEMINI_MODEL
GEMINI_MAX_OUTPUT_TOKENS
GEMINI_TIMEOUT_MS
ALLOWED_ORIGIN


做法 B：繼續使用 Workers Static Assets

這條路也可以，但需要改架構：

1. 要新增一個真正的 Worker 入口檔，例如 src/index.js。
2. 要新增 wrangler.jsonc 或 wrangler.toml。
3. 要把 /api/ideabox 寫進 Worker fetch handler。
4. 要讓 Worker 同時服務 static assets 和 API。

這比 Pages 複雜。

除非你很確定想走 Workers Static Assets，不然先不要走這條。


你現在應該怎麼判斷？

如果 Cloudflare 畫面上寫的是：

Workers
Static Assets
Worker

那你大機率在錯的地方。

如果畫面上寫的是：

Pages
Deployments
Functions
Variables and Secrets

那才是我們要設定的地方。


最簡單結論

看到「無法將變數新增到只有靜態資產的 Worker 中」時：

不要在那個 Worker 裡設定。
請改到 Cloudflare Pages 專案裡設定。

你的 GitHub repo 是：
https://github.com/a9181873/dky-tools

Pages 的 Root directory 請填：
dky_tw
